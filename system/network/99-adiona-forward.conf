# Enable IPv4 forwarding so the Pi routes the Wi-Fi AP clients (the Quest) out to
# the Ethernet uplink. NetworkManager's 'shared' mode sets up the NAT masquerade;
# this guarantees forwarding is on regardless of boot ordering.
net.ipv4.ip_forward=1
